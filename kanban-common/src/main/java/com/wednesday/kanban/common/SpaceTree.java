package com.wednesday.kanban.common;

public class SpaceTree {
    /** 树根*/
    private SpaceTreeNode root;

    /**
     * 构造函数
     */
    /*public SpaceTree(SpaceResult rootSpace)
    {
        root = new SpaceTreeNode(rootSpace);
    }*/

    /**
     * 遍历多叉树  
     *
     * @param spaceTreeNode 多叉树节点
     * @return
     */
/*    public void iteratorTree(SpaceTreeNode spaceTreeNode)
    {


        if(spaceTreeNode != null)
        {
            for (SpaceTreeNode index : spaceTreeNode.getChildList())
            {


                if (index.getChildList() != null && index.getChildList().size() > 0 )
                {
                    iteratorTree(index);
                }
            }
        }


    }*/
}
